{
  "$comment": "Bundler-resolved DOM code, like vinta-ai-maestro's ui/tsconfig.json. The `@/*` path exists for the shadcn CLI (components.json aliases); committed sources import relatively so a consumer's bundler needs no alias.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "target": "ES2023",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "types": ["node", "vite/client"],
    "paths": { "@/*": ["./src/*"] },
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "noEmit": true
  },
  "include": ["src", "tests", "vitest.config.ts"]
}
